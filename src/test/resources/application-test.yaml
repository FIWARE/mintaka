micronaut:

  http:
    services:
      entities:
        path: ngsi-ld/v1
        url: http://localhost:8026
      temporalRetrieval:
        path: /
        url: http://localhost:8080

logger:
  level:
    root: ERROR
    org:
      fiware:
        mintaka:
          persistence: DEBUG

test:
  clientId: entities

datasources:
  default:
    host: localhost